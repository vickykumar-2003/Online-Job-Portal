import {Link,useNavigate} from 'react-router-dom';import {getUser,logout} from '../utils/auth';
export default function Navbar(){const n=useNavigate(),u=getUser();return <header className="navbar"><div className="nav"><Link className="brand" to="/">JobSphere</Link><nav><Link to="/">Jobs</Link>{u?.role==='recruiter'&&<Link to="/recruiter">Dashboard</Link>}{u&&<Link to="/applications">Applications</Link>}{u?<button className="btn outline" onClick={()=>{logout();n('/login')}}>Logout</button>:<><Link to="/login">Login</Link><Link className="btn primary" to="/register">Register</Link></>}</nav></div></header>}
